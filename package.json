{
  "name": "07_noc-app",
  "version": "0.0.1",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "tsnd --respawn --clear src/app.ts",
    "build": "rimraf ./dist && tsc",
    "start": "npm run build && node dist/app.js"
  },
  "keywords": [],
  "author": "humberto de la cruz dominguez",
  "license": "MIT",
  "description": "Aplicacion de monitoreo",
  "devDependencies": {
    "@types/node": "^24.0.3",
    "@types/nodemailer": "^6.4.17",
    "rimraf": "^6.0.1",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "cron": "^4.3.1",
    "nodemailer": "^7.0.3"
  }
}
